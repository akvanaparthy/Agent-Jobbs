# ========================================
# PAGINATION CONFIGURATION
# ========================================

# Maximum pages to extract per keyword/location combination
# Controls how many pages of job listings to scrape
# 
# Options:
#   - Specific number: 1, 5, 10, 15, 20, etc.
#   - 'all' or 'unlimited': Extract all available pages (use with caution!)
#
# Default: 15
#
# Examples:
#   MAX_PAGES_TO_EXTRACT=1      # Only first page (fastest, good for testing)
#   MAX_PAGES_TO_EXTRACT=15     # Extract 15 pages (recommended)
#   MAX_PAGES_TO_EXTRACT=all    # Extract all pages until last (may be slow)
#
# Note: ZipRecruiter typically shows 20-25 jobs per page
# 15 pages ≈ 300-375 jobs per keyword/location combo

MAX_PAGES_TO_EXTRACT=15

# ========================================
# RELATED CONFIGURATIONS
# ========================================

# SEARCH_PAGES_PER_COMBO (deprecated, kept for backward compatibility)
# This was the old setting, now replaced by MAX_PAGES_TO_EXTRACT
SEARCH_PAGES_PER_COMBO=1

# ========================================
# PERFORMANCE TIPS
# ========================================
#
# For Testing:
#   MAX_PAGES_TO_EXTRACT=1  (quick test with first page only)
#
# For Daily Job Hunt:
#   MAX_PAGES_TO_EXTRACT=5-10  (balance between coverage and speed)
#
# For Comprehensive Search:
#   MAX_PAGES_TO_EXTRACT=15-20  (thorough search, may take longer)
#
# For Maximum Coverage:
#   MAX_PAGES_TO_EXTRACT=all  (extracts until no more pages, use sparingly)
#
# ========================================
